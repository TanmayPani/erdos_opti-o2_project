<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-06-02 — do not edit manually*

### Dataset
*~900 tokens/session saved*
- Primary dataset: `datasets/BeaverCreekWA_EssDive_26Jun2019-30Sep2024/data/` — 553,845 rows × 15 columns; load with polars (not pandas).
- Datetime column is `'Datetime'` (polars `Datetime(time_unit='us')`); O2/temperature columns contain nulls — call `.drop_nulls()` or `.fill_null()` before any `group_by` aggregation to avoid `MarimoExceptionRaisedError`.

### Marimo Workflow
*~900 tokens/session saved*
- Validate with `.venv/bin/marimo check exploratory.py`; export with `timeout 300 .venv/bin/marimo export html <file> -o <out.html>`.
- Exit code 0 does NOT mean success — grep the output for `MarimoExceptionRaisedError`; cell failures are reported in stderr even when the HTML is written.
- Marimo's auto-formatter rewrites `.py` files on save; always re-read `exploratory.py` immediately before any `Edit` call to avoid 'file modified since read' errors.

### Environment
*~600 tokens/session saved*
- Use `.venv/bin/python` for all Python invocations (never `python3` or `python`); install packages with `uv add <pkg>`.
- Python 3.14 is in use; packages like `matplotlib`, `altair`, and `scipy` may not be installed — probe with `.venv/bin/python -c "import <pkg>"` before assuming availability.

<!-- headroom:learn:end -->
